package dungeonforge.events;

/** WEEK 5 (US-3.3) -- every kind of thing Combat (or the game) can announce. */
public enum EventType {
    LEVEL_ENTERED,
    ROOM_CLEARED,
    MONSTER_DIED,
    STRATEGY_CHANGED,
    DELVE_SURVIVED,
    MESSAGE
}
